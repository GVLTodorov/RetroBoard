namespace RetroBoard.Domain.Templates;

/// <summary>Fixed column layout chosen at board creation (see <see cref="TemplateCatalog"/>) — the
/// template can never change once a board exists.</summary>
public enum TemplateType
{
    WentWellDidntWork,
    StartStopContinue,
    MadSadGlad,
    FourLs,
}
